Local examples
==============

Use this directory for sample personal files that should not be installed by default.

- Keep secrets and machine-specific values out of the repository root install flow.
- Copy from here into your own unmanaged local files when needed.
